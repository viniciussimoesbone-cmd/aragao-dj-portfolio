{
  "name": "nivora",
  "version": "0.1.0",
  "private": true,
  "description": "NIVORA — sistema de gestão de negócio (Fase 0: núcleo)",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "prisma": "^5.20.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
